# Rust build artifacts
/target
**/*.rs.bk

# clove runtime cache (also ignored per-repo by .clove/.gitignore for consuming repos)
.clove/index.db
.clove/index.db-shm
.clove/index.db-wal
.clove/index.db.tmp
.clove/daemon.sock
.clove/daemon.pid
.clove/reindex.lock
.clove/daemon.lock

# Criterion / fuzz build output and crash artifacts (the seed corpus under
# /fuzz/corpus IS committed — it is the T-X02 regression input).
/fuzz/target
/fuzz/artifacts

# OS / editor cruft
.DS_Store
*.swp

# Generated TUI screenshots (tooling output; not committed)
/docs/screenshots/
