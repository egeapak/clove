//! cloved: the optional clove daemon (M3+).
//!
//! Watches `.clove/issues/`, keeps the SQLite index incrementally fresh, and can
//! opt-in to git auto-sync. Never required. Stub in M0; see
//! IMPLEMENTATION_PLAN.md M3.

fn main() {
    eprintln!("cloved is not implemented until M3 (see docs/IMPLEMENTATION_PLAN.md)");
    std::process::exit(1);
}
